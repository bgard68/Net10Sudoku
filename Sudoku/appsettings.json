{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_AllowedHosts": "Host header allow-list. '*' accepts any Host, which permits host-header injection and cache poisoning. Override with the AllowedHosts app setting when the hostname changes; a wrong value makes every request 400, and the post-deploy smoke test catches that.",
  "AllowedHosts": "blazor-sudoku-net10.azurewebsites.net;localhost;127.0.0.1;[::1]"
}
